# تقسیم وظایف پروژه ۱

## معرفی

این سند تقسیم وظایف اعضای تیم برای پروژه «طراحی سامانه پردازشی چندهسته‌ای و پیاده‌سازی قرارداد انسجام حافظه» را مشخص می‌کند.

اعضای تیم:

- احسان
- آراد
- شهاب

هدف از این تقسیم‌بندی، توزیع عادلانه کار، کاهش وابستگی مستقیم اعضا به یکدیگر و فراهم‌کردن امکان توسعه موازی بخش‌های مختلف پروژه است.

---

## معماری کلی پروژه

سامانه نهایی باید شامل اجزای زیر باشد:

- دو هسته پردازنده
- یک حافظه نهان داده سطح اول اختصاصی برای هر هسته
- یک گذرگاه مشترک
- یک حافظه اصلی مشترک
- کنترل‌کننده انسجام مبتنی بر قرارداد MESI
- پشتیبانی از دستور `cpuid`
- پشتیبانی از دستورات اتمی `lr.w`، `sc.w` و `amoadd.w`
- واحد رزرو مستقل برای هر هسته
- برنامه‌های آزمایشی و ابزارهای جمع‌آوری نتایج عملکرد

تقسیم وظایف به‌گونه‌ای انجام شده است که سه بخش اصلی پروژه، یعنی زیرساخت سامانه، حافظه نهان و انسجام، و دستورات اتمی، بین سه نفر تقسیم شوند.

---

# ۱. وظایف احسان

## حوزه مسئولیت

**معماری کلان سامانه، گذرگاه مشترک، زیرساخت پروژه و یکپارچه‌سازی نهایی**

## وظایف اصلی

### ۱. راه‌اندازی پروژه

- بررسی ساختار کد پایه و مشخص‌کردن روش اجرای شبیه‌سازی
- آماده‌سازی ساختار پوشه‌های پروژه
- تنظیم دستورهای build، run و test
- اضافه‌کردن فایل README اولیه
- تعریف قواعد نام‌گذاری ماژول‌ها، سیگنال‌ها و تست‌ها

### ۲. طراحی معماری سطح بالا

- ساخت ماژول Top-Level سامانه دوهسته‌ای
- ایجاد دو نمونه از هسته پردازنده
- ایجاد محل اتصال دو حافظه نهان خصوصی
- اتصال سامانه به حافظه اصلی مشترک
- تعریف مسیر ارتباطی Core، Cache، Bus و Memory
- تعریف پارامتر یا سیگنال `core_id` برای هر هسته

### ۳. طراحی و پیاده‌سازی گذرگاه

- تعریف انواع تراکنش‌های گذرگاه
- پیاده‌سازی درخواست‌های `BusRd` و `BusRdX`
- طراحی و پیاده‌سازی داوری Round-Robin
- مدیریت مالکیت گذرگاه
- هدایت درخواست‌ها به حافظه اصلی یا حافظه نهان مقابل
- تعریف سیگنال‌های پاسخ، انتظار و پایان تراکنش
- فراهم‌کردن امکان Snooping برای هر دو حافظه نهان

### ۴. آماده‌سازی Interfaceهای مشترک

احسان مسئول تثبیت Interfaceهای زیر است:

- Core به Cache
- Cache به Bus
- Bus به Memory
- Cache به Core برای ابطال رزرو
- درخواست دسترسی انحصاری برای عملیات اتمی
- پاسخ موفق یا ناموفق عملیات حافظه

Interfaceها باید پیش از شروع توسعه موازی آراد و شهاب مستند شوند.

### ۵. تست زیرساخت

- تست روشن‌شدن و اجرای همزمان دو هسته
- تست دسترسی ساده هر هسته به حافظه
- تست ارسال همزمان درخواست توسط دو هسته
- تست داوری Round-Robin
- ساخت Mock یا Stub برای Cache و Core
- آماده‌سازی Testbench پایه برای توسعه مستقل اعضا

### ۶. یکپارچه‌سازی نهایی

- ادغام شاخه آراد و شهاب در شاخه توسعه
- بررسی سازگاری Interfaceها
- اجرای تست‌های سطح سامانه
- تشخیص محل خطاهای Integration
- هماهنگی برای رفع خطاهای داخل هر ماژول
- آماده‌سازی نمودار معماری کلی
- آماده‌سازی بخشی از نمودارهای زمانی مربوط به گذرگاه

## خروجی قابل تحویل احسان

- Top-Level دوهسته‌ای قابل اجرا
- گذرگاه مشترک پیاده‌سازی‌شده
- داور Round-Robin
- Interfaceهای مستندشده
- Testbench پایه
- تست‌های داوری و دسترسی مشترک
- سند معماری کلان
- نسخه یکپارچه نهایی پروژه

---

# ۲. وظایف آراد

## حوزه مسئولیت

**حافظه نهان داده، ماشین حالت MESI و منطق Snooping**

## وظایف اصلی

### ۱. پیاده‌سازی حافظه نهان

- پیاده‌سازی یک حافظه نهان داده اختصاصی برای هر هسته
- استفاده از ساختار Direct-Mapped
- درنظرگرفتن اندازه بلوک ۱۶ بایت
- پیاده‌سازی Tag، Valid، Data و وضعیت MESI برای هر خط
- پیاده‌سازی سیاست Write-Back
- پیاده‌سازی سیاست Write-Allocate

### ۲. پیاده‌سازی ماشین حالت MESI

- پیاده‌سازی حالت `Modified`
- پیاده‌سازی حالت `Exclusive`
- پیاده‌سازی حالت `Shared`
- پیاده‌سازی حالت `Invalid`
- تعریف انتقال حالت در خواندن محلی
- تعریف انتقال حالت در نوشتن محلی
- تعریف انتقال حالت در دریافت `BusRd`
- تعریف انتقال حالت در دریافت `BusRdX`
- مدیریت انتقال مالکیت خط بین دو هسته

### ۳. مدیریت تراکنش‌های انسجام

- درخواست `BusRd` در Cache Miss خواندنی
- درخواست `BusRdX` برای به‌دست‌آوردن مالکیت انحصاری
- Flush داده در حالت `Modified`
- نوشتن داده تغییریافته در حافظه اصلی
- ابطال نسخه موجود در حافظه نهان هسته مقابل
- پاسخ به Snooping گذرگاه
- تشخیص وجود نسخه مشترک در هسته دیگر

### ۴. ارتباط با عملیات اتمی

- دریافت درخواست مالکیت انحصاری از Core
- تضمین ورود خط هدف به وضعیت مناسب پیش از نوشتن
- تولید سیگنال ابطال رزرو هنگام نوشتن هسته مقابل
- ارسال وضعیت تکمیل عملیات حافظه به Core
- رعایت اتمی‌بودن تراکنش در سطح Cache و Bus

### ۵. آمار و تحلیل عملکرد

- شمارش Cache Hit
- شمارش Cache Miss
- شمارش Flush
- شمارش Invalidation
- شمارش انتقال‌های حالت MESI
- فراهم‌کردن خروجی قابل استفاده برای گزارش نهایی

### ۶. تست‌های مستقل

- تست `I -> E`
- تست `I -> S`
- تست `E -> M`
- تست `S -> M`
- تست `M -> S`
- تست `M -> I`
- تست Flush
- تست Invalidation
- تست خواندن مشترک یک خط توسط دو هسته
- تست نوشتن یکی از هسته‌ها روی خط مشترک
- تست ابطال رزرو در اثر نوشتن هسته مقابل

## خروجی قابل تحویل آراد

- ماژول Cache کامل
- ماشین حالت MESI
- منطق Snooping
- Flush و Invalidation
- اتصال Cache به Interface گذرگاه
- شمارنده‌های عملکرد
- تست‌های انتقال حالت و انسجام
- توضیح ماشین حالت برای گزارش

---

# ۳. وظایف شهاب

## حوزه مسئولیت

**توسعه ISA، دستورات اتمی، واحد رزرو و برنامه‌های آزمایشی**

## وظایف اصلی

### ۱. پیاده‌سازی دستور `cpuid`

- اضافه‌کردن Decode دستور
- دریافت شناسه هسته
- نوشتن شناسه هسته در ثبات مقصد
- نوشتن تست مستقل برای هسته صفر و یک

### ۲. پیاده‌سازی دستور `lr.w`

- Decode دستور
- خواندن کلمه ۳۲ بیتی از حافظه
- نوشتن مقدار خوانده‌شده در `rd`
- ثبت آدرس رزروشده
- معتبرکردن بیت رزرو هسته
- مدیریت Stall یا انتظار برای تکمیل دسترسی حافظه

### ۳. پیاده‌سازی دستور `sc.w`

- Decode دستور
- بررسی معتبر بودن رزرو
- بررسی تطابق آدرس رزروشده
- انجام نوشتن در صورت معتبر بودن رزرو
- نوشتن مقدار صفر در `rd` در صورت موفقیت
- نوشتن مقدار یک در `rd` در صورت شکست
- پاک‌کردن رزرو پس از اجرای دستور
- جلوگیری از نوشتن در صورت رزرو نامعتبر

### ۴. پیاده‌سازی دستور `amoadd.w`

- Decode دستور
- خواندن مقدار قبلی حافظه
- ذخیره مقدار قبلی در `rd`
- جمع مقدار قبلی با `rs2`
- نوشتن نتیجه در همان آدرس
- اجرای عملیات به‌صورت Read-Modify-Write تفکیک‌ناپذیر
- درخواست مالکیت انحصاری خط پیش از نوشتن

### ۵. واحد رزرو

- طراحی واحد رزرو مستقل برای هر هسته
- ذخیره آدرس رزروشده
- نگهداری بیت اعتبار رزرو
- دریافت سیگنال ابطال رزرو از Cache
- ابطال رزرو در اثر نوشتن هسته مقابل
- پاک‌کردن رزرو در شرایط لازم

### ۶. تغییرات خط لوله و واحد کنترل

- اعمال تغییرات لازم در Decode
- اعمال تغییرات لازم در Execute
- اعمال تغییرات لازم در Memory Stage
- مدیریت Write Back دستورات جدید
- مدیریت وابستگی‌های داده‌ای
- اضافه‌کردن Stall یا کنترل لازم
- جلوگیری از ناقص اجراشدن عملیات اتمی

### ۷. برنامه‌های آزمایشی

- تست مستقل `cpuid`
- تست مستقل `lr.w`
- تست موفق `sc.w`
- تست ناموفق `sc.w`
- تست `amoadd.w`
- برنامه ساخت قفل با `lr.w/sc.w`
- برنامه شمارنده مشترک دوهسته‌ای
- برنامه جمع اتمی
- تست ناحیه بحرانی
- تست Race Condition بدون همگام‌سازی
- تست نسخه اصلاح‌شده با عملیات اتمی

## خروجی قابل تحویل شهاب

- Decode و اجرای دستور `cpuid`
- پیاده‌سازی `lr.w`
- پیاده‌سازی `sc.w`
- پیاده‌سازی `amoadd.w`
- واحد رزرو هر هسته
- تغییرات خط لوله و واحد کنترل
- تست‌های مستقل دستورات
- برنامه‌های آزمایشی دوهسته‌ای
- توضیح دستورات اتمی برای گزارش

---

# مرز مسئولیت‌ها

برای جلوگیری از تداخل وظایف، مرزهای زیر رعایت می‌شوند:

- احسان مسئول پیاده‌سازی داخلی Cache نیست.
- آراد مسئول Decode و اجرای دستورات اتمی نیست.
- شهاب مسئول ماشین حالت MESI و داوری Bus نیست.
- هر نفر مسئول رفع باگ داخل ماژول خودش است.
- احسان مسئول تشخیص خطاهای اتصال و هماهنگی Integration است، نه بازنویسی ماژول دیگران.
- هر تغییری در Interfaceهای مشترک باید با هماهنگی هر سه نفر انجام شود.

---

# ترتیب شروع کار

## مرحله اول: فونداسیون توسط احسان

تا پیش از شروع توسعه موازی، احسان باید موارد زیر را آماده کند:

- ساختار پروژه
- روش build و test
- Top-Level اولیه
- Interfaceهای مشترک
- انواع درخواست‌های Bus
- شناسه هسته
- سیگنال ابطال رزرو
- Mockهای مورد نیاز
- Testbench پایه

پس از این مرحله، آراد و شهاب می‌توانند کار خود را مستقل شروع کنند.

## مرحله دوم: توسعه موازی

- آراد روی Cache و MESI کار می‌کند.
- شهاب روی ISA و عملیات اتمی کار می‌کند.
- احسان Bus، Arbiter، تست زیرساخت و مستندات معماری را تکمیل می‌کند.

## مرحله سوم: ادغام اولیه

- اتصال Cache آراد به Bus احسان
- اتصال دستورات اتمی شهاب به Cache
- تست خواندن و نوشتن ساده دوهسته‌ای
- تست ابطال خطوط
- تست ابطال رزرو

## مرحله چهارم: تست نهایی

- تست قفل نرم‌افزاری
- تست شمارنده مشترک
- تست `amoadd.w`
- تست رقابت دو هسته
- استخراج آمار Cache و Bus
- آماده‌سازی نمودارهای زمانی
- تکمیل گزارش نهایی

---

# ساختار پیشنهادی شاخه‌های Git

- `main`: نسخه پایدار و قابل تحویل
- `develop`: شاخه ادغام پروژه
- `feature/ehsan-foundation`
- `feature/ehsan-bus`
- `feature/arad-cache`
- `feature/arad-mesi`
- `feature/shahab-cpuid`
- `feature/shahab-atomic`

## قوانین Git

- توسعه مستقیم روی `main` ممنوع است.
- هر قابلیت در یک شاخه جدا توسعه داده شود.
- هر تغییر از طریق Pull Request وارد `develop` شود.
- پیش از Merge، تست‌های مربوط به همان بخش باید موفق باشند.
- تغییر Interface مشترک باید در Pull Request به‌وضوح نوشته شود.
- Commitها کوچک و دارای پیام مشخص باشند.
- هر نفر مستندات بخش خودش را همراه کد به‌روزرسانی کند.

---

# معیار اتمام وظایف

یک وظیفه زمانی تمام‌شده محسوب می‌شود که:

- کد آن پیاده‌سازی شده باشد.
- پروژه بدون خطای Build اجرا شود.
- تست مستقل مربوط به آن وجود داشته باشد.
- تست مربوطه موفق باشد.
- Interface آن مستند شده باشد.
- Pull Request آن بررسی شده باشد.
- کد وارد شاخه `develop` شده باشد.

---

# مسئولیت گزارش نهایی

هر نفر مسئول نوشتن توضیحات فنی بخش خودش است:

- احسان: معماری دوهسته‌ای، Bus، Arbiter، اتصال حافظه و نمودار کلی
- آراد: Cache، MESI، انتقال حالت‌ها، Flush، Invalidation و آمار Cache
- شهاب: `cpuid`، دستورات اتمی، واحد رزرو، تغییرات خط لوله و برنامه‌های آزمون

ویرایش و یکپارچه‌سازی نهایی گزارش با همکاری هر سه نفر انجام می‌شود.

---

# جمع‌بندی تقسیم کار

| عضو | مسئولیت اصلی | تحویل کلیدی |
|---|---|---|
| احسان | معماری کلان، Bus، Arbiter و Integration | Top-Level، گذرگاه، Interfaceها و نسخه یکپارچه |
| آراد | Cache و MESI | حافظه نهان منسجم، Snooping، Flush و Invalidation |
| شهاب | ISA و عملیات اتمی | `cpuid`، `lr.w`، `sc.w`، `amoadd.w` و واحد رزرو |

این تقسیم‌بندی با هدف توزیع متوازن پیچیدگی، میزان کدنویسی، تست و مسئولیت نهایی انجام شده است.
